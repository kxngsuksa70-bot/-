# 🚀 GitHub Upload Guide

คุณมี 3 วิธีในการอัพโหลด code ขึ้น GitHub:

---

## วิธีที่ 1: GitHub Desktop (แนะนำ - ง่ายที่สุด) ⭐

### ขั้นตอน:

1. **ดาวน์โหลด GitHub Desktop**
   - ไปที่: https://desktop.github.com
   - คลิก "Download for Windows"
   - ติดตั้งโปรแกรม

2. **Login GitHub**
   - เปิด GitHub Desktop
   - คลิก "Sign in to GitHub.com"
   - ใส่ username/password

3. **Add Repository**
   - คลิก "Add" → "Add Existing Repository"
   - เลือกโฟลเดอร์: `C:\Users\Window 10 Home\Downloads\puyfai`
   - คลิก "Add Repository"

4. **Create Repository on GitHub**
   - คลิก "Publish repository"
   - ตั้งชื่อ: `teachmap-pwa` 
   - เลือก: Public (หรือ Private ถ้าต้องการ)
   - **ยกเลิก**ติ๊ก "Keep this code private" ถ้าต้องการ public
   - คลิก "Publish repository"

5. **เสร็จ!** 🎉
   - Code ของคุณถูกอัพโหลดขึ้น GitHub แล้ว
   - URL จะเป็น: `https://github.com/your-username/teachmap-pwa`

---

## วิธีที่ 2: ติดตั้ง Git + Command Line

### ขั้นตอน:

1. **ดาวน์โหลด Git**
   - ไปที่: https://git-scm.com/download/win
   - ดาวน์โหลดและติดตั้ง
   - ใช้ค่าเริ่มต้นทั้งหมด (Next ไปเรื่อยๆ)

2. **Restart Terminal**
   - ปิด PowerShell/CMD
   - เปิดใหม่อีกครั้ง

3. **Configure Git** (ครั้งแรก)
   ```bash
   git config --global user.name "Your Name"
   git config --global user.email "your.email@example.com"
   ```

4. **Initialize Git Repository**
   ```bash
   cd "C:\Users\Window 10 Home\Downloads\puyfai"
   git init
   git add .
   git commit -m "Initial commit - TeachMap PWA with Supabase"
   ```

5. **สร้าง Repository บน GitHub**
   - ไปที่: https://github.com/new
   - ชื่อ: `teachmap-pwa`
   - คลิก "Create repository"

6. **Push ขึ้น GitHub**
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/teachmap-pwa.git
   git branch -M main
   git push -u origin main
   ```
   
   (แทน `YOUR_USERNAME` ด้วย username GitHub ของคุณ)

---

## วิธีที่ 3: อัพโหลดผ่าน GitHub Web (ไม่แนะนำ - ช้า)

### ขั้นตอน:

1. **สร้าง Repository**
   - ไปที่: https://github.com/new
   - ชื่อ: `teachmap-pwa`
   - คลิก "Create repository"

2. **อัพโหลดไฟล์**
   - คลิก "uploading an existing file"
   - ลากไฟล์ทั้งหมดจากโฟลเดอร์ `puyfai`
   - **ยกเว้น:**
     - โฟลเดอร์ `__pycache__`
     - โฟลเดอร์ `backup_*`
     - ไฟล์ `.env` (ห้ามอัพโหลด!)
     - ไฟล์ `.db`, `*.sqlite`
   - คลิก "Commit changes"

---

## ⚠️ สำคัญ! ไฟล์ที่ห้ามอัพโหลด:

```
❌ .env (มี password)
❌ __pycache__/ (Python cache)
❌ *.db, *.sqlite (database files)
❌ backup_*/ (backup folders)
❌ node_modules/ (ถ้ามี)
```

**ดีแล้ว:** เรามีไฟล์ `.gitignore` ที่จะป้องกันไฟล์เหล่านี้อัตโนมัติ!

---

## 🎯 หลังจากอัพโหลดเสร็จ:

คุณจะได้ GitHub URL เช่น:
```
https://github.com/your-username/teachmap-pwa
```

**เก็บ URL นี้ไว้!** เพราะจะใช้ใน Railway ในขั้นตอนต่อไป

---

## ❓ คำถามที่พบบ่อย:

**Q: Git คืออะไร?**
A: โปรแกรมสำหรับจัดการ source code (version control)

**Q: GitHub คืออะไร?**
A: เว็บไซต์สำหรับเก็บ code บนคลาวด์

**Q: ใช้วิธีไหนดี?**
A: **วิธีที่ 1 (GitHub Desktop)** ง่ายและเหมาะกับผู้เริ่มต้น

**Q: ต้องทำทุกครั้งที่แก้ code ไหม?**
A: ใช่ แต่พอทำครั้งแรกแล้ว ครั้งต่อไปจะง่ายขึ้น (แค่ commit + push)

---

**พร้อมแล้วเลือกวิธีที่ชอบได้เลย!** บอกผมเมื่อทำเสร็จ แล้วเราจะไป deploy บน Railway ต่อ! 🚀
