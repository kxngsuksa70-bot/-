# 🔧 แก้ปัญหา: ngrok เข้าไม่ได้

## ปัญหาที่พบ

### ❌ Error: "1 simultaneous ngrok agent sessions"
```
Your account is limited to 1 simultaneous ngrok agent sessions
```

**สาเหตุ:** มี ngrok session เปิดอยู่แล้ว (ngrok ฟรีเปิดได้ครั้งละ 1 tunnel)

## ✅ วิธีแก้

### วิธีที่ 1: ปิด ngrok ที่เปิดอยู่

**ใช้ Task Manager:**
1. `Ctrl + Shift + Esc`
2. หา `ngrok.exe`
3. คลิกขวา → End Task

**ใช้ Command:**
```bash
taskkill /F /IM ngrok.exe
```

### วิธีที่ 2: ตรวจสอบ sessions

ดูที่:
https://dashboard.ngrok.com/agents

ปิด sessions ที่ไม่ใช้

### วิธีที่ 3: รัน app.py ใหม่

**ปิด terminal ทั้งหมด** แล้วรันใหม่:
```bash
cd web
python app.py
```

---

## 📋 Checklist สำหรับให้เพื่อนเข้าได้

เมื่อรัน `python app.py` ต้องเห็น:

✅ **ข้อความนี้:**
```
🌍 PUBLIC URL (Internet): https://xxxx.ngrok-free.app
   แชร์ URL นี้ให้เพื่อนทั่วโลกเข้าได้!
```

✅ **Flask กำลังรัน:**
```
* Running on http://0.0.0.0:5000
```

✅ **ไม่มี error**

---

## 🌍 วิธีให้เพื่อนเข้า

1. คัดลอก URL ตรงๆ (เช่น `https://abc123.ngrok-free.app`)
2. แชร์ให้เพื่อน
3. เพื่อนเปิด browser → วาง URL
4. **สำคัญ:** ถ้าเห็นหน้า ngrok warning กด **"Visit Site"**

---

## ⚠️ ข้อควรระวัง

**ngrok ฟรีมีข้อจำกัด:**
- ✅ 1 tunnel พร้อมกัน
- ✅ URL เปลี่ยนทุกครั้งรัน
- ✅ มีหน้า warning (ต้องกด Visit Site)
- ❌ ไม่มี custom domain

**ถ้าต้องการ:**
- URL คงที่: อัพเกรด ngrok ($10/เดือน)
- หลาย tunnels: อัพเกรด ngrok
- Production: Deploy จริง (Railway/Render)

---

## 🆘 ยังไม่ได้?

**ตรวจสอบ:**
1. ✅ Server กำลังรัน (`python app.py`)
2. ✅ เห็น PUBLIC URL ใน terminal
3. ✅ ไม่มี ngrok อื่นรันอยู่
4. ✅ URL ที่แชร์ถูกต้อง (copy-paste ตรงๆ)
5. ✅ เพื่อนกด "Visit Site" ในหน้า ngrok

**ถ้ายังไม่ได้:**
- ส่ง screenshot terminal ให้ดู
- ตรวจสอบ https://dashboard.ngrok.com/agents
