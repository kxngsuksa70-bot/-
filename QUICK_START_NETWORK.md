# 🌐 วิธีให้คนอื่นเข้าดูเว็บไซต์ TeachMap

## ✅ Server พร้อมใช้งาน!

Server ตั้งค่าให้รับการเชื่อมต่อจากภายนอกแล้ว

## 📍 IP Address ของเครื่องคุณ

จาก `ipconfig` พบ IP หลายตัว ให้ใช้:
- **WiFi/LAN หลัก:** `192.168.28.215` หรือ `192.168.191.117`

## 🚀 วิธีให้เพื่อนเข้าใช้งาน

### 1. รัน Server (ต้องรันทุกครั้ง)
```bash
cd c:\Users\Nanoatom\Downloads\puyfai\web
python app.py
```

### 2. บอก URL ให้เพื่อน

**ตัวเลือกที่ 1:** (ลองก่อน)
```
http://192.168.28.215:5000
```

**ตัวเลือกที่ 2:** (ถ้าตัวแรกไม่ได้)
```
http://192.168.191.117:5000
```

### 3. เพื่อนเปิดบนมือถือ/คอม
- เชื่อม **WiFi เดียวกัน**
- เปิด browser พิมพ์ URL ด้านบน

## 🔐 Account ทดสอบ

**อาจารย์:**
- `teacher1` / `1234`

**นักศึกษา:**
- `student1` / `1234`

## 🔥 แก้ Firewall (ถ้าเชื่อมต่อไม่ได้)

Firewall rule ถูกสร้างแล้ว! ถ้ายังไม่ได้:

**เปิด Python ใน Firewall:**
1. กด `Windows + R` → `firewall.cpl`
2. "Allow an app..." → "Change settings"
3. หา "Python" ติ๊กถูก **Private** และ **Public**
4. คลิก OK

## ⚠️ สำคัญ

- ✅ Server ต้องรันอยู่ (terminal เปิดค้าง)
- ✅ เพื่อนต้องเชื่อม WiFi เดียวกัน
- ✅ Port 5000 ต้องไม่ถูก block
- ❌ ไม่สามารถเข้าจาก Internet ภายนอก

## 🧪 ทดสอบ

**จากเครื่องตัวเอง:**
```
http://localhost:5000
```

**จากเครื่องอื่น:**
```
http://192.168.28.215:5000
```
