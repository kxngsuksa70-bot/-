# 🔑 วิธีตั้งค่า ngrok Authtoken

## ปัญหา
```
ERROR: authentication failed: Usage of ngrok requires a verified account and authtoken.
```

## ✅ วิธีแก้ (2 นาที)

### ขั้นตอน:

#### 1. สมัคร ngrok (ฟรี)
https://dashboard.ngrok.com/signup

#### 2. รับ Authtoken
https://dashboard.ngrok.com/get-started/your-authtoken

คัดลอก token (ตัวอย่าง: `2abc123xyz...`)

#### 3. ตั้งค่า Authtoken

**วิธีที่ 1: ใช้ใน app.py (แนะนำ)**

แก้ไข `web/app.py` บรรทัด 439:
```python
NGROK_AUTH_TOKEN = "YOUR_TOKEN_HERE"  # วาง token ที่นี่
```

**วิธีที่ 2: ใช้ command**
```bash
ngrok config add-authtoken YOUR_TOKEN_HERE
```

**วิธีที่ 3: ใช้ setup_ngrok.py**
1. แก้ `setup_ngrok.py` ใส่ token
2. รัน: `python setup_ngrok.py`

#### 4. รัน server
```bash
cd web
python app.py
```

จะได้ URL:
```
🌍 PUBLIC URL (Internet): https://abc123.ngrok-free.app
```

#### 5. แชร์ URL ให้เพื่อน!

---

## 🔍 ตรวจสอบว่าตั้งค่าแล้ว

```bash
ngrok config check
```

---

## ⚠️ หมายเหตุ

- Authtoken ใช้แค่ครั้งเดียว (บันทึกไว้)
- ฟรี: 1 tunnel พร้อมกัน
- URL จะเปลี่ยนทุกครั้งที่รัน (ฟรี)

---

## 🆘 ถ้ายังไม่ได้

1. ตรวจสอบ token ถูกต้อง
2. ลอง restart terminal
3. ลบ `~/.ngrok2/` แล้วตั้งค่าใหม่
