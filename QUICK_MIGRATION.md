# 🚀 Quick Migration Steps

## คุณทำมาแล้ว:
✅ สร้างตารางใน Supabase สำเร็จ

## ต้องทำต่อ (3 ขั้นตอน):

### 1️⃣ แก้ไขไฟล์ .env (ใส่ Supabase Password)

เปิดไฟล์ `.env` แล้วแก้ไขบรรทัดนี้:
```
SUPABASE_PASSWORD=YOUR_DATABASE_PASSWORD_HERE
```

เปลี่ยนเป็น password จริงที่ได้จาก Supabase

**วิธีหา Password:**
1. ไปที่: https://supabase.com/dashboard/project/hbbqwcesmwqnfgkmdayp/settings/database
2. หา Connection string → คลิก URI
3. คัดลอก password จาก connection string
   หรือคลิก "Reset database password"

### 2️⃣ ตรวจสอบ MySQL Password (ถ้าต้องการ)

เปิดไฟล์ `migrate_mysql_to_supabase.py` บรรทัด 18-22:
```python
MYSQL_CONFIG = {
    'host': 'localhost',
    'user': 'root',
    'password': '1234',  # แก้เป็น password ของคุณ
    'database': 'teachmap_db'
}
```

### 3️⃣ รัน Migration Script

เปิด Terminal/Command Prompt แล้วรัน:
```bash
cd "C:\Users\Window 10 Home\Downloads\puyfai"
python migrate_mysql_to_supabase.py
```

---

## คาดว่าจะเห็น:

```
============================================================
🚀 MySQL → Supabase Migration Tool
============================================================
✅ เชื่อมต่อ MySQL สำเร็จ
✅ เชื่อมต่อ Supabase สำเร็จ

📋 กำลัง migrate ตาราง teachers...
  ✅ Migrate 5 teachers สำเร็จ

📋 กำลัง migrate ตาราง students...
  ✅ Migrate 10 students สำเร็จ

📋 กำลัง migrate ตาราง schedule...
  ✅ Migrate 20 schedules สำเร็จ

🎉 ข้อมูลทั้งหมดถูก migrate ไปยัง Supabase แล้ว!
```

---

## หมายเหตุ:

- ⚠️ แก้ `.env` ให้ใส่ **SUPABASE_PASSWORD** ก่อนรัน
- ⚠️ ตรวจสอบว่า MySQL server รันอยู่
- ✅ Script จะดึงข้อมูลทั้งหมดจาก MySQL มาใส่ Supabase

---

**พร้อมแล้วรันได้เลย!** 
หลังจาก migrate เสร็จ → บอกผมแล้วเราจะ deploy บน Railway ต่อ! 🚀
