# วิธีให้คนอื่นเข้าดูเว็บไซต์ TeachMap

## ✅ การตั้งค่าเบื้องต้น

Server ถูกตั้งค่าให้รับการเชื่อมต่อจากภายนอกแล้ว (`host='0.0.0.0'`)

## 📱 วิธีให้คนอื่นเข้าใช้งาน

### 1. หา IP Address ของเครื่องคุณ

**Windows:**
```powershell
ipconfig
```
หา **IPv4 Address** ในส่วน **Wireless LAN adapter Wi-Fi** หรือ **Ethernet**

ตัวอย่าง: `192.168.1.100`

**หรือใช้คำสั่งสั้นๆ:**
```powershell
ipconfig | Select-String -Pattern "IPv4"
```

### 2. รัน Flask Server

```bash
cd web
python app.py
```

จะเห็นข้อความ:
```
Mobile access: http://<your-ip>:5000
Desktop access: http://localhost:5000
```

### 3. แชร์ URL ให้เพื่อน

ให้เพื่อนที่อยู่ในเครือข่าย WiFi/LAN เดียวกันเปิด:
```
http://YOUR_IP_ADDRESS:5000
```

**ตัวอย่าง:**
- ถ้า IP คุณคือ `192.168.1.100`
- เพื่อนเปิด: `http://192.168.1.100:5000`

## 🔥 ตั้งค่า Windows Firewall (ถ้าเชื่อมต่อไม่ได้)

### วิธีที่ 1: อนุญาต Python ใน Firewall

1. กด `Windows + R`
2. พิมพ์ `firewall.cpl` กด Enter
3. คลิก **"Allow an app or feature through Windows Defender Firewall"**
4. คลิก **"Change settings"**
5. หา **"Python"** และติ๊กถูกทั้ง **Private** และ **Public**
6. คลิก **OK**

### วิธีที่ 2: เปิด Port 5000 (ใช้ PowerShell แบบ Admin)

```powershell
New-NetFirewallRule -DisplayName "Flask TeachMap" -Direction Inbound -Protocol TCP -LocalPort 5000 -Action Allow
```

## 📋 Account สำหรับทดสอบ

**อาจารย์:**
- Username: `teacher1`
- Password: `1234`

**นักศึกษา:**
- Username: `student1`
- Password: `1234`

## 🌐 ทดสอบการเชื่อมต่อ

### จากเครื่องเดียวกัน:
```
http://localhost:5000
```

### จากเครื่องอื่นในเครือข่ายเดียวกัน:
```
http://YOUR_IP:5000
```

### จากมือถือ (เชื่อม WiFi เดียวกัน):
```
http://YOUR_IP:5000
```

## ⚠️ ข้อควรระวัง

1. **ต้องอยู่ WiFi/LAN เดียวกัน** - คนที่จะเข้าต้องเชื่อมต่อเครือข่ายเดียวกับเครื่อง server
2. **ไม่สามารถเข้าจาก Internet** - วิธีนี้ใช้ได้แค่ภายในเครือข่ายเดียวกันเท่านั้น
3. **ต้องเปิด Server อยู่** - ถ้าปิด terminal/command prompt server จะหยุดทำงาน

## 🚀 สำหรับการ Deploy จริง (Production)

ถ้าต้องการให้คนทั่วไปเข้าได้จาก Internet:

### ตัวเลือก:
1. **Railway.app** - ฟรี, ง่าย, รองรับ Python
2. **PythonAnywhere** - ฟรี, เหมาะสำหรับ Flask
3. **Render** - ฟรี, auto-deploy จาก GitHub
4. **Heroku** - เสียเงิน แต่เสถียร

บอกผมถ้าต้องการคู่มือ deploy ครับ!

## 🔍 Troubleshooting

**ปัญหา: เชื่อมต่อไม่ได้**
- ✅ ตรวจสอบ IP address ถูกต้อง
- ✅ ตรวจสอบ server กำลังรันอยู่
- ✅ ตรวจสอบ firewall
- ✅ ตรวจสอบเชื่อม WiFi/LAN เดียวกัน

**ปัญหา: Firewall Block**
- ✅ อนุญาต Python ใน Windows Firewall
- ✅ หรือเปิด Port 5000

**ปัญหา: IP เปลี่ยน**
- IP อาจเปลี่ยนเมื่อเริ่มเครื่องใหม่
- ต้องเช็ค `ipconfig` ใหม่ทุกครั้ง
